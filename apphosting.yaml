# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Configurations optimized for efficiency and free tier limits
  maxInstances: 2
  minInstances: 0
  memory: 512Mi
  cpu: 1

env:
  - variable: NODE_ENV
    value: production
